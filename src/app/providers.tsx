"use client";

import React from "react";

// এখানে theme/tooltip/QueryClient/Redux etc. রাখতে পারো
export default function Providers({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
